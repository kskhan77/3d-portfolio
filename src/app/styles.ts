import type { AppPanelKey } from './types'

export const appPanelClassNames: Record<AppPanelKey, string> = {
  hero: 'overlay-panel hero-panel',
  teleport: 'overlay-panel teleport-panel',
  status: 'overlay-panel status-panel',
}
